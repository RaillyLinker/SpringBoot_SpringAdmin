server:
  port: 8081

spring:
  boot:
    admin:
      notify:
        mail:
          to: testReceiver@gmail.com # ???? ??? ??? ?? ??? ??
  security:
    user:
      name: adminId # Admin ?? ???
      password: adminPw # Admin ?? ??

  mail:
    host: smtp.gmail.com # SMTP ?? ???
    port: 587 # SMTP ?? ??
    username: testSender@gmail.com # SMTP ?? ??? ??? (???)
    password: fo4weigkugmbct1q # SMTP ?? ??? ???? (? ????)
    default-encoding: UTF-8
    properties:
      mail:
        debug: true
        mime:
          charset: UTF-8
        transport:
          protocol: smtp
        smtp:
          auth: true # ??? ?? ?? ?? (??? : false)
          timeout: 5000 # Socket Read Timeout ??(ms) (??? : ???)
          starttls:
            enable: true # StartTLS ??? ?? (??? : false)
            required: true